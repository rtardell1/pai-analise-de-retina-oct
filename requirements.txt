# Dependências fixadas do projeto PAI — análise de retina (OCT + RD).
#
# Modelos carregados via timm (hf_hub:<repo_id>), não via transformers:
#   - bitfount/RETFound_MAE_OCT_CNV_DME_DRU  (OCT, 4 classes)
#   - bitfount/RETFound_DR_IDRID             (fundo, 5 graus ICDR)
# Ambos são ViT-Large/16 (RETFound), licença CC-BY-NC-4.0 (uso não-comercial).

torch==2.12.0
torchvision==0.27.0
timm==1.0.27
pillow==12.2.0
numpy==2.4.6
pandas==3.0.3
matplotlib==3.10.9
scikit-learn==1.9.0
huggingface_hub==0.36.2
jsonschema==4.26.0
python-dotenv==1.2.3
ipywidgets==8.1.9
ipykernel==7.3.0
