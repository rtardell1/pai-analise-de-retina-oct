# Dependências fixadas do projeto PAI — laudo interpretativo de glaucoma a partir de OCT.
#
# pymupdf: leitura e anonimização do PDF do relatório Zeiss Cirrus.
# numpy/scipy/pillow: o pipeline de imagem (projeções, componentes conexos, geometria).
# pandas/matplotlib: tabelas e figuras do notebook.
#
# Não há dependência de aprendizado de máquina. A avaliação do RETFound que motivou essa
# decisão está resumida na seção 14 do notebook; o código dela foi removido.

pymupdf==1.28.2
pillow==12.2.0
numpy==2.4.6
scipy==1.18.1
pandas==3.0.3
matplotlib==3.10.9
ipykernel==7.3.0
